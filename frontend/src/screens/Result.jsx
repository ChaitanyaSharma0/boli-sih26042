// Screen 3 — simplified Hindi, Santali translation, and audio per language.
// Wired in PLAN.md Phase 7.
export default function Result({ onBack }) {
  return (
    <section>
      <h2>3. Result</h2>
      <p>Simplified text, translation and audio — not built yet.</p>
      <button onClick={onBack}>Back</button>
    </section>
  );
}
