# Base URL of the deployed backend. Vite inlines this at BUILD time, not
# at runtime, so changing it means rebuilding — on Vercel, set it in the
# project's Environment Variables and redeploy.
# Unset, the app falls back to http://127.0.0.1:8000 for local work.
VITE_API_BASE=
