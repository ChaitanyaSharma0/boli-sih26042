HF_TOKEN=            # required for IndicTrans2 gated model
LLM_API_KEY=         # for pedagogy simplification step
LLM_PROVIDER=        # "anthropic" | "openai" | "gemini" (only gemini implemented)
DATABASE_PATH=./db/boli.sqlite
TESSERACT_CMD=       # optional: path to tesseract.exe if it is not on PATH
